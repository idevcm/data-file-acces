package MainPackage.Excepciones;

/**
 * Class that inherits from Exception and is used to create custom exceptions.
 */
public class CustomizedException extends Exception{
        public CustomizedException(String e){
            super(e);
        }
}
